// Canonical hook exports — use these paths in all new code.

export { usePermissions } from './usePermissions';

// Data-fetching hooks (TanStack Query)
export {
  useClients,
  useTasks,
  useAssets,
  useNotifications,
  useDashboardStats,
} from './queries';
