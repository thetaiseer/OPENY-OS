export default function DocsArchivePage() {
  return (
    <div className="p-6 sm:p-8">
      <h1 className="text-2xl font-semibold" style={{ color: 'var(--text)' }}>archive</h1>
      <p className="text-sm mt-2" style={{ color: 'var(--text-secondary)' }}>
        OPENY DOCS archive workspace is isolated and owner-only.
      </p>
    </div>
  );
}
