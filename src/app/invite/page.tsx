import { SimpleScreen } from '@/new-ui/simple-screen';
export default function Page() { return <SimpleScreen title="Invitation" subtitle="Your invitation workflow now uses the rebuilt interface." ctaLabel="Accept invitation" />; }
